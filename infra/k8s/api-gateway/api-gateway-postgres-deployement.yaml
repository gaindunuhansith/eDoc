apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway-db
  namespace: edoc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway-db
  template:
    metadata:
      labels:
        app: api-gateway-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: api_gateway_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: edoc-secrets
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: edoc-secrets
                  key: DB_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U user -d api_gateway_db
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-db
  namespace: edoc
spec:
  selector:
    app: api-gateway-db
  ports:
    - port: 5432
      targetPort: 5432

